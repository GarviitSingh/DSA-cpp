#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

/*int main() {
	vector <int>vec;  //vector created with size 0
	vec.push_back(1); //pushbackused to insert values from back
	vec.push_back(2);
	vec.push_back(3);
	vec.push_back(4);
	vec.push_back(5);
	
	vec.emplace_back(6); //this is also used to push values from back 
	
	vec.pop_back(); // this will delete the last value from the vector 

	vec.erase(vec.begin() + 2); // it used to erase the element from vector 
	// if we want to erase a range- vec.erase(start,end) - (vec.begin() +1 ) (vec.begin() +3)

	vec.insert(vec.begin() + 2 , 100);  //used to insert values in middle

	//vec.clear();  //it clears the values
	// capacity remains same
	
	
	for (int val : vec) {
		cout << val << " ";
	}
		
	cout << endl << vec.size() << endl;  // fucntion used to chcck vector size 
	cout << vec.capacity() << endl;  // fucntion used to check vector capacity 
	cout << "Value at index 3 is- " << vec[3] << endl; // used to find the position
	cout << "Value at front- " << vec.front() << endl; //used to find the front index 
	cout << "Value at back- " << vec.back() << endl; //used to find the back front 
	cout << "is empty- " << vec.empty() << endl;  //used to check if vector is empty or not 

	return 0;
}
*/
